from smarketsim import metalog
import numpy
from numpy.random import multivariate_normal

def _assign_stock_keys(changes):
    return

class Model:
    def __init__(self):
        return
    
    def fit(self, changes, date, look_back, mlog_dim):
        self.stock_keys = _assign_stock_keys(changes)
        self
        cov = numpy.cov()
        return
